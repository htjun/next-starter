# Status

Current hot-path state. Keep this file short.

- State: generic starter baseline.
- Active plan: none.
- Current customization surfaces: `app/layout.tsx`, `app/page.tsx`, `.env.example`, `package.json`, `scripts/init-starter.mjs`.
- If work spans sessions: add one file under `docs/plans/active/` and link it here.
